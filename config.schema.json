{
  "pluginAlias": "SomfyProtect",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Modern Homebridge plugin for Somfy Protect home security systems. For help and documentation please see the [GitHub repository](https://github.com/yourusername/homebridge-somfy-protect).",
  "footerDisplay": "If you have multiple sites, check your Homebridge logs to find the site IDs.",
  "schema": {
    "type": "object",
    "required": ["username", "password"],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Somfy Protect",
        "description": "The name that will appear in your Homebridge log."
      },
      "username": {
        "title": "Username",
        "type": "string",
        "format": "email",
        "placeholder": "your.email@example.com",
        "description": "Your Somfy Protect account email address"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "placeholder": "your-password",
        "description": "Your Somfy Protect account password"
      },
      "siteId": {
        "title": "Site ID (Optional)",
        "type": "string",
        "placeholder": "Leave blank for default site",
        "description": "If you have multiple sites, specify which site to use. Check logs to find site IDs."
      },
      "pollingInterval": {
        "title": "Polling Interval (ms)",
        "type": "integer",
        "default": 10000,
        "minimum": 5000,
        "maximum": 60000,
        "description": "How often to check for status updates (milliseconds). Default is 10000 (10 seconds)."
      },
      "debug": {
        "title": "Debug Logging",
        "type": "boolean",
        "default": false,
        "description": "Enable debug logging for troubleshooting"
      },
      "httpPort": {
        "title": "🌐 HTTP API Port",
        "type": "integer",
        "default": 8581,
        "minimum": 0,
        "maximum": 65535,
        "description": "Port for the HTTP API used by automation plugins. Set to 0 to disable."
      },
      "httpToken": {
        "title": "🔒 HTTP API Token",
        "type": "string",
        "placeholder": "Optional security token",
        "description": "Optional token for securing the HTTP API. If set, requests must include 'Authorization: Bearer YOUR_TOKEN' header."
      },
      "enableSwitch": {
        "title": "🔘 Enable Arm/Disarm Switch",
        "type": "boolean",
        "default": false,
        "description": "Adds a Switch to the alarm accessory. Useful for Apple Home automations (e.g. arm when you leave, disarm when you arrive)."
      },
      "switchArmMode": {
        "title": "Arm mode for Switch ON",
        "type": "string",
        "default": "armed",
        "enum": ["armed", "partial"],
        "description": "Security level applied when the switch is turned ON. 'armed' = full away mode, 'partial' = stay/home mode."
      }
    }
  },
  "layout": [
    {
      "type": "flex",
      "flex-flow": "row wrap",
      "items": [
        {
          "type": "flex",
          "flex-flow": "column",
          "items": ["username", "password"]
        }
      ]
    },
    {
      "type": "fieldset",
      "title": "Advanced Settings",
      "expandable": true,
      "expanded": false,
      "items": [
        "siteId",
        "pollingInterval",
        "debug"
      ]
    },
    {
      "type": "fieldset",
      "title": "HTTP API (for Automation)",
      "expandable": true,
      "expanded": false,
      "items": [
        "httpPort",
        "httpToken"
      ]
    },
    {
      "type": "fieldset",
      "title": "Arm/Disarm Switch",
      "expandable": true,
      "expanded": false,
      "items": [
        "enableSwitch",
        "switchArmMode"
      ]
    }
  ]
}
